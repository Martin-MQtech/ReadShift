# ReadShift 项目执行手册（L1 · SOP）

> 规则版本：`governance-2026-08-13`（自 2026-08-12 起历次治理、排坑、成书的经验已收敛为本版）
> 定位：这是一套**可复用的标准作业程序（SOP）**。只要按本手册的「开工前体检 → 规范 → 构建 → QA → 浏览器验收 → 成书」顺序走，普通模型也能照流程完成同类双语电子书制作，无需高智商、无需临场考古。
> 本手册已吸收本项目全部血泪教训，把"曾经事后才修的问题"前置为"开工前必查的规矩"。

---

## 0. 一句话模型

每一步都是固定动作：**先体检、再写规范源、再一键构建、再用 QA 闸门、再浏览器验收、最后合成成书。** 六章（含卷首序言与附录）最终合成一本离线可读的单一双语 HTML 电子书。

---

## 1. 开工前必读（顺序固定）

1. `project.json` —— 机器可读事实、权限、各章路径/状态（唯一事实源）
2. 本手册（L1）—— 全局 SOP、契约、验收
3. 目标章节的 `执行手册.md`（L2）—— 该章局部事实、履历、验收证据
4. `docs/LESSONS_LEARNED.md` + `docs/LESSONS_LEARNED_CH2.md` —— 历史坑与铁律（**不要**从里面复制旧命令）

**冲突裁决顺序**：`project.json` > 本手册 > 章节手册。若冲突，停止生产，升级给高级模型定夺，绝不自行选有利版本。

---

## 2. 项目边界与三层数据

- 唯一章节根：`output/chapters/`，正好 6 个工作区（`chap-01`~`chap-06`）。
- 全书合成产物放：`output/Samples/`（命名 `{首-尾}章合成-Chapter-{a}-{b}.html`）。
- 任意一层，禁止建 `output/full/`、全局 `output/images/`、`preview` 分支、备份目录、第二份手册或同职能旁支；同章只保留一份 HTML。

每章固定三层数据：

- **D0 `raw_source/`**：只读原始证据（OCR/文本层），**永不修改**。
- **D1 `source/*.md`**：唯一人工编辑源，**所有内容修复都改这里**。同时是未来二次创作（音频/MP3/更多内容）的**素材资产**，必须始终干净、可机器解析。
- **D2 章节 HTML**：由 renderer 从 D1 自动生成，**手改 D1 后重渲染即可**。

> 硬规矩：**改内容只动 D1，D2 永远由 renderer 生成**，不做任何手工 HTML 编辑。

---

## 3. 开工前资产体检（第一道防线的闸门，不可跳过）

任何一次生产/修改，**开工前先跑一遍全家体检**，把历史遗留问题在动笔前拦下。这是本项目用整本书的弯路换来的第一条铁律。

```bash
# 对目标章（或全书所有章）逐章：
python3 workdir/qa_gate_v3.py --chapter N        # 单章全绿才开工
# 体检 D1 健康度（见 docs/HEALTHCHECK.sh 或手工 python）：
#   1) span/div/rhetoric-note 结构是否全部闭合
#   2) 是否有占位符(英文翻译。/TBD/TODO)、连续空行、裸文本、繁体
#   3) 正文首段前是否有孤立中文章名大标题(# 第X章 / 裸 附录)
```

**体检不达标 → 先修 D1 到全绿，再进入生产。** 不要带着脏 source 开始，否则问题会在合成时叠加爆炸（实战教训：第二章 36 个未闭合 span、source 脏数据，都是硬拖到合成才暴露、返工成本最高的）。

---

## 4. 权限边界与职责分工（写清例外，避免绕路）

| 动作 | 允许对象 |
|------|---------|
| 改各章 `source/*.md`、`images/` | 生产 Agent |
| 记各章/本手册的履历与验收 | 生产 Agent |
| 修共享工程缺陷（`render_html_v9.js` / `template.ejs` / `qa_gate_v3.py`） | 高级模型（但必须全章回归） |
| 改 `project.json`、D0 原始素材 | 不得（除非用户明确要求） |

> **关键澄清（本版新增）**：当问题根因在共享 renderer/template/QA 时，生产 Agent **不是"一句话甩给高级模型"，而是**：记录证据 → 明确根因与最小修法 → 提交给高级模型。共享文件修复必须逐章回归（`--chapter 1..6` + 合成），不许只为了某一章。
> 不提交、不推送、不发布，除非用户明确要求。

---

## 5. 内容标准契约（含"本文档沉淀的所有不变量"）

### 5.1 正文中英双语
- 主文严格「一段中文，紧接一段英文」交替；禁止连续英文、一个中文挂两个英文、跨页机械切分。
- **章节大标题全局约定**：每章正文开头的章节标题统一由 renderer 注入的**双语章标题卡**（`.chapter-nav--start`）承担——中文大标题（`title_zh`）+ 斜体英文副标题（`title_en`）+ ✦ 装饰分割线 + 著者。**正文第一段之前不得再出现任何孤立中文章名大标题**（`# 第X章` / 裸`第X章`/`附录` 都不行）。发现必删。
- 小节标题用 `## ` 语法（renderer 会登记 `sub-*` 锚点并进目录）。

### 5.2 二创卡片三块式（rebook-card）
- **Cheat Sheet**：`### Cheat Sheet · 商业语汇` + 编号 `**term** — 中文` + `**中文解释**` + `**商业造句**`。
  - ⚠️ **term 行与 `**中文解释**` 之间绝不允许空行**，否则 renderer 读不到释义、词卡高亮变 0（实战教训）。
- **修辞赏析**：`### 修辞赏析` + `<span class="rhetoric-note"><span class="zh">…</span><span class="en">…</span></span>`，`.`zh`/`.en` 必须成对、非空、语义对应；**不保留同文裸段**。
- **背景知识延伸**：`### 背景知识延伸` + `<div class="knowledge-note"><span class="zh">…</span><span class="en">…</span></div>`。
- 三板块中文字号较正文小、楷体斜体；英文更小、Georgia/Times 粗斜体，弱于正文。

### 5.3 源料资产（D1）清洁基线
- 无占位符（英文翻译。/TBD/TODO/待补充）、无水印、无裸文本残留、无繁体。
- 段落间用**单个空行**分隔（`\n\n`），不出现 `\n{3,}` 连续空行。
- `div`/`span`/`rhetoric-note` 全部闭合。

---

## 6. 视觉与交互契约（前端固定项）

- 顶栏左 `☰ 目录`、中 `源起于文本，想象于无限`、右 `ReadShift · 私人书房`；右下角 `↑ 顶部`。
- 目录为**两级导航**：顶层章列表（每章一个链接 `#chap-XX` 跳章首）+ 下方"本章结构"当前章小节（随滚动/跳转更新并高亮）；序言（P1/P2/P3）并入第一章。
- 目录支持：再点收起、遮罩关闭、条目跳转后关闭、Esc 关闭；不锁死正文滚动。
- **点目录外空白即关闭（2026-08-13 实现）**：目录面板外铺一层 `position:fixed; inset:0; z-index:99` 的**透明**遮罩（.secnav__overlay），卡片 `z-index:101` 浮于其上。点卡片外任意区域 → 触底遮罩 → 关闭目录；点卡片内条目仍正常跳转。遮罩**纯透明、无背景、无虚化**，不改变任何视觉。此行为在 template 层实现，单章与全书（`--chapter` / `--chapters`）均生效。
- 只原生 JS、系统字体、本地资源；禁 CDN / Google Fonts / Alpine；单文件图片用 data URI。
- 控制台 0 错误；桌面与移动视口正常。

---

## 7. 多章合成（成书）SOP

这是「合成样章 → 全合成」的一键流程，也是把分散单章变成整本书的收尾。

```bash
# 合成任意区间的章（顺序 = 传入顺序，renderer 按页码排序）
node workdir/render_html_v9.js --chapters 1,2,3          # 三章样板
node workdir/render_html_v9.js --chapters 1,2,3,4,5,6    # 六章全书
# 对合成产物 QA（用 --html 指定）
python3 workdir/qa_gate_v3.py --html output/Samples/一-六章合成-Chapter-1-6.html
```

合成时 renderer 自动处理：
- 按页码聚合多个 source 目录 → 单一 HTML。
- 目录卡（toc-card）中指向**未被合成**的章链接自动裁剪（消灭断头锚点）。
- 两级目录注入章列表 + 小节树。

> **成书前必做**：先对每个参与合成的章跑 `--chapter N` 单章 QA 全绿，再合成；合成本本身只做聚合与裁剪，不负责修脏数据。

---

## 8. QA 与出货定义（第二道 / 第三道闸门）

QA 检查的是**刚生成的 canonical HTML**（不是 preview/缓存）。`qa_gate_v3.py` 已用 HTMLParser 处理嵌套结构（**禁止回退到正则解析嵌套 HTML**）。至少覆盖：转义 / 标签闭合 / 内容纯净 / 中文重复 / 二创卡片结构 / rhetoric-note / 目录锚点可达 / 目录小节完整性 / 双语框架 / 品牌规范 / 无 CDN / manifest 基线 / 秘密扫描。

**"完成"四要素（缺一不可）**：
1. canonical source（D1）已核实。
2. canonical HTML（D2）由当前 renderer 重建。
3. QA v3 对该 HTML 全部通过（0 FAIL；允许原书排比句的 G3-03 警告）。
4. 桌面 + 移动浏览器验收通过（目录两级/跳转、顶部按钮、控制台 0 错误），且记录证据。

---

## 9. 状态机

`needs_revalidation` → `in_progress` → `release_candidate`（QA 全过） → `accepted`。
- `blocked_until_golden_baseline` / `pending` / `pending_manifest_review` 为未开跑态。
- 升级给高级模型的触发项：原书证据冲突、跨章结构变化、无法判定的标题/缺页、renderer/QA/模板缺陷、QA 误报漏报、需改全局标准。

---

## 10. 复盘：把"事后修的弯路"转为"一开始的规矩"（SOP 的价值所在）

下面是本项目真实踩过、并已在上文固化为规矩的坑。**未来的工作流已经把这些前移为「开工前体检」和「标准格式模板」，因此不需要再踩一遍**：

| 曾经事后才修 | 现在一开始就规避 |
|---|---|
| 第二章 rhetoric-note 残缺 → 36 未闭合 span | 开工前体检 span/rhetoric 闭合（§3） |
| source 混 `# 第X章` 孤立标题 | §5.1 正文首段前禁章名（§3 体检） |
| 导航标题硬编码回退"第一章" | 章名数据注入，无 if/else（模板契约） |
| 词卡高亮 0（term 与释义空行） | §5.2 term/中文解释无空行规范 |
| QA 正则误判嵌套 HTML | §8 用 HTMLParser，禁正则解析嵌套 |
| source 脏数据（多空行/占位符） | §3 体检 + §5.3 清洁基线 |
| 目录缺小节但 QA 全绿 | QA 加 G4-02 目录小节完整性 + 浏览器验收 |
| 合成时断头锚点 | §7 合成自动裁剪 toc-card 越界章链接 |
| 未受控批量正则改 source 丢翻译 | 教训（LESSONS_LEARNED_CH2）：改 D1 先备份、用 parser |

> 方法论（沉淀自第五章与全书实践）：**大任务按章节原子化拆解 + 话题隔离多会话推进**。每次只处理一个被指定章节，不跨章改共享代码；每个问题走「证据核查 → 1:1 叙事流 → 二创植入 → 受控构建 + QA → 手册写回」闭环。

---

## 11. 安全规则

- 密钥只从环境变量/安全配置读取，禁止写入脚本/手册/日志/JSON 批次。
- 发现明文密钥立即停止、清除、查 Git 历史，并由所有者在供应商后台轮换。
- 未经用户明确要求，不提交/推送/发布/删除既有成果。

---

## 12. 手册维护规则

- 本手册只写**现行标准与规矩**，不写逐页履历。
- 章节手册只写局部事实、例外、履历、验收证据，不复制全局标准。
- 历史故障与铁律写入 `docs/LESSONS_LEARNED*.md`。
- 路径/页码/输出名/状态只在 `project.json` 维护。
- `docs/HANDOFF_FULLBOOK_20260813.md` 是给"接手 Agent 完成全书的交接与排查"专用，包含当前资产清单与残留待办。

---

## 13. 当前全书状态（2026-08-13 成书完成）

- 六章 source（D1）已全部清洗达标（结构完整、无脏数据），可直接作二创素材。
- 单章与六章全书均 QA 全绿；成书 `output/Samples/一-六章合成-Chapter-1-6.html`（101 页）。
- **六章单章已全部 QA 全绿**（含第一章 G4-01 断头链接已修复，2026-08-13）。
- 章节标题卡双语装饰、两级目录导航、掌级跳转、源料清洁基线均已落地。
- 残留待办：见下文 §14 交接语第 4 步（按 SOP 重查一遍，命中残留即修到全绿）。

---

## 14. 任务交接语（直接复制给接手 Agent）

> 用途：把本任务完整交给新的 Agent，让其**不臆测、按既有 SOP 快速重查核对**，无需重新考古。
> 使用方法：把下面整段作为新话题的首条消息发给接手 Agent。

```
你正在维护 ReadShift《张忠谋自传》上册双语 HTML 项目。工作目录 = 当前仓库根目录。
本任务不是从头制作，而是【按既定 SOP 对既有成果做一遍完整核对/复检】，修复残留问题，最终产出合规成书。

【接手必读顺序】
1. project.json —— 机器可读事实、权限、各章路径/状态（唯一事实源）
2. 根目录 执行手册.md —— 全局 SOP（重点看：§3 开工前体检、§5 内容契约、§7 多章合成、§8 QA 与出货定义、§14 本交接语）
3. 目标章节的 output/chapters/chap-0N-*/执行手册.md —— 该章局部事实/履历
4. docs/LESSONS_LEARNED*.md —— 历史坑与铁律（只作参考，不要照抄里面的旧命令）

【你要做的事，按顺序】
0. (必做) 不要从头重做。先跑一遍体检，判断资产现状：
   bash docs/HEALTHCHECK.sh            # 六章 D1 源料体检（未闭合/占位符/多空行/孤立章名/繁体）
   再逐章跑 QA：python3 workdir/qa_gate_v3.py --chapter 1..6
1. 逐章核对：六章单章 HTML 是否由当前 renderer 从 D1 重建、QA 是否 0 失败。
2. 核对全书成书：node workdir/render_html_v9.js --chapters 1,2,3,4,5,6
   后 python3 workdir/qa_gate_v3.py --html output/Samples/一-六章合成-Chapter-1-6.html。
3. 按 §5 内容契约逐项核对：中英 1:1、正文首段前无孤立章名、二创卡片三块式、D1 清洁。
4. 命中残留（当前已知仅为：附录章存在两个 HTML——权威 `附录-Appendix.html` 与旧 `第六章-Chapter-6.html`，确认后删冗余并让 renderer `--chapter 6` 输出到权威名）：按 §3/§4/§5 的规矩修复 D1，重渲染，QA 到全绿。修复共享 renderer/template/QA 需全章回归。
5. 浏览器验收：目录两级跳转、章标题卡双语装饰、顶部按钮、控制台 0 错误、移动端视口。
6. 把真实构建时间、输出哈希、QA 报告、浏览器证据写回对应章与根执行手册的履历/状态。

【完工标准（缺一不可）】
- 六章 D1 体检干净、QA 0 失败;
- 全书成书单一 HTML 生成、QA 0 失败、浏览器验收通过;
- 两级目录每章可跳转、当前章小节正确、无断头链接、标题卡中英双语+装饰线;
- 无残留待办 / 无废弃旁支 / 无未声明交付副本。

【底线】
- 只改 D1 source/*.md 与手册履历; 共享文件缺陷→记录证据+最小修法后升级给高级模型(且须全章回归)。
- 未经证据齐全, 不得写“完成/QA全绿/accepted”。
- 不提交、不推送、不发布, 除非用户明确要求。
```
