# ReBook Production Dependencies
# Core PDF processing
PyMuPDF>=1.23.0           # PDF to image conversion

# OCR (local fallback)
pytesseract>=0.3.10        # Tesseract wrapper

# HTTP requests for API calls
requests>=2.31.0
aiohttp>=3.8.0             # Async for parallel API calls

# Template rendering
markdown-it-py>=3.0.0      # Markdown parsing
Jinja2>=3.1.0              # Template engine

# PDF output (optional)
weasyprint>=60.0           # HTML to PDF conversion
# OR
pypandoc>=1.11             # Pandoc wrapper

# LLM API clients (uncomment as needed)
# openai>=1.0.0            # OpenAI/GPT-4o
# anthropic>=0.18.0        # Claude
# zhipuai>=2.0.0           # 智谱GLM
# dashscope>=1.0.0         # 阿里通义千问
